# July 2026

## 2026-07-05 - PER-343 SD-card safety fixture

Objective: create a recoverable local test fixture before the app reads camera
media.

Method: detected `/Volumes/Untitled/DCIM`, then ran
`python3 scripts/copy_last_24h_media.py --source /Volumes/Untitled/DCIM --destination .local-test-data/sd-last-24h-20260705-1748 --hours 24`.
The helper copies into the local workspace, preserves relative `DCIM` paths, and
writes `reproduction.txt` plus `copy_manifest.json`.

Observations: initial copy included macOS AppleDouble `._*` files because they
share media extensions. The helper was corrected to skip names starting with
`._`, and the fixture manifest was regenerated.

Result: `.local-test-data/sd-last-24h-20260705-1748/` initially contained 477
real camera files, 29,698,155,690 bytes: 216 JPG, 216 RAF, and 45 MOV. No `._*`
files remained. Later review-agent follow-up refreshed this fixture to match
the current 24-hour mtime window exactly; see the fixture refresh entry below.

Interpretation: the fixture is suitable for app testing while preserving the SD
card as the recoverable source. See
`decisions/agent/pending/PER-343-test-fixture-window-mtime.md` for the
mtime-window assumption.

## 2026-07-05 - PER-343 copied-fixture smoke test

Objective: validate the scanner and exporter against copied real camera media,
not synthetic files and not the live SD card.

Method: ran
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" swift test --filter FixtureSmokeTests`.

Observations: the first run returned zero scanned items because the scanner used
`FileManager`'s hidden-file skipping option and the fixture lives under the
hidden `.local-test-data/` directory. The scanner now uses explicit media
extension filtering and skips only AppleDouble `._*` files.

Result: the smoke test passed. It scanned the copied fixture and exported one
real JPG plus one real MOV passthrough segment to
`.local-test-data/smoke-export-core/`; `manifest.json` had an empty failures
array.

Interpretation: the app core can read the copied camera data and write a
recoverable export without touching the source card.

## 2026-07-05 - PER-343 GUI smoke test

Objective: validate the native macOS GUI against the copied fixture using
Computer Use.

Method: built `dist/SDReview.app` with `scripts/package_app.sh`, then launched
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" dist/SDReview.app/Contents/MacOS/SDReview`.

Observations: the app opened on the copied fixture path, scanned 242 reviewable
items for the selected date range, displayed real JPG media, and loaded a real
MOV in AVPlayer. `K` marked a photo keep and auto-advanced. `C` opened crop mode
with the rule-of-thirds overlay, and Return applied the crop and marked the item
keep. Video `I`/`O`/`A` created a segment and marked the clip keep.

Correction: the first GUI video banking pass reset playback to zero after
banking; this contradicted the spec's "playback simply continues" requirement.
Segment edits now preserve the active AVPlayer.

Result: after the correction, banking a segment left playback running near the
current playhead. The export sheet displayed keep/reject tallies, a prominent
undecided warning, destination, flat-folder option, estimated size, and export
button. No GUI export was run to `~/Pictures`; file-writing was already
validated by the core copied-fixture smoke export.

## 2026-07-05 - PER-343 review-agent follow-up

Objective: address review-agent findings against the spec and record remaining
deltas for user review.

Method: spawned two read-only review agents. One reviewed spec/wireframe drift;
the other reviewed runtime and source-safety risks. Local follow-up ran
`swift test` and
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" swift test --filter FixtureSmokeTests`.

Observations: reviewers found high-impact issues in export destination safety,
manifest completeness, photo segment mutation, filtered auto-advance, session
reload behavior, crop orientation, and smoke-export reproduction persistence.
They also identified larger spec gaps around preview caching, card fingerprint,
timestamp validation, mouse affordances, and export progress UI.

Result: source-tree/source-volume export guards, complete manifest entries,
video-only segment guards, filtered auto-advance, scan/session merge, whole-day
scan ranges, RAW/HEIF/problem status, crop orientation baking, video re-export
on rerun, and smoke-export reproduction regeneration were implemented. Remaining
larger deltas were added to `decisions/agent/pending/`.

## 2026-07-05 - PER-343 card fingerprint and preview cache

Objective: close the material spec gaps around card-based session identity and
disk-backed preview/thumbnail generation.

Method: implemented `CardFingerprint` and `MediaPreviewCache`, wired scanner
results and session persistence to the fingerprint, and updated the SwiftUI
review panes to consume cached photo previews plus filmstrip thumbnails and
video posters.

Observations: the first cache worker implementation captured the UI model from
detached tasks and produced Swift 6 sendability warnings. The worker now runs
cache generation in detached tasks and updates the main-actor model through a
separate task after completion.

Result: `swift test` passed with 11 tests, one fixture-gated test skipped. The
copied-camera smoke command
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" swift test --filter FixtureSmokeTests`
passed against real JPG/MOV data, including preview, thumbnail, and video poster
generation from the copied fixture.

Interpretation: the previous card fingerprint and preview-cache pending
decisions are resolved and archived. Remaining pending deltas concern the test
fixture mtime window, mouse affordances, video timestamp validation, and export
progress UI.

## 2026-07-05 - PER-343 polish gaps

Objective: close the remaining material M4/spec gaps before the final review
agent pass.

Method: added exporter progress callbacks, GUI progress/failure display, a
Rejects-grid jump from the export sheet, a cache-backed grid skim mode, a
problems sheet, persisted settings for video time offset/export handles/cache
limit, scanner validation for timezone-sized video/photo timestamp deltas, and
optional mouse controls for video scrubbing, segment edge adjustment, and crop
handle resizing.

Observations: the first compile caught actor isolation around reading the video
offset inside the detached scanner task. Capturing the offset value before
spawning the task kept the scanner work off the main actor without crossing
actor-isolated state.

Result: `swift test` passed with 12 tests, one fixture-gated test skipped. The
copied-camera smoke command
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" swift test --filter FixtureSmokeTests`
passed against the copied real camera fixture.

Interpretation: the export-progress, mouse-affordance, and video-time pending
decisions are resolved and archived. The remaining pending decision is the
fixture mtime-window assumption used when copying the last 24 hours of card
contents.

## 2026-07-05 - PER-343 review-agent safety follow-up

Objective: address final review-agent findings before delivery.

Method: fixed the copied-fixture refresh path, export resumability, session
save atomicity, session fallback after fingerprint changes, preview-cache
atomic/self-healing writes, crop UI/export coordinate mapping, visible
resume/reset controls, source-unavailable warning, original crop aspect, and
source-frame video stepping.

Observations: the fixture directory had stale files from an earlier 24-hour
window. The copy helper now removes stale media under the test destination and
refuses destinations inside the source tree or on the same mounted source
volume. The refreshed fixture now has 380 manifest-matched files:
171 JPG, 171 RAF, 38 MOV, 24,178,347,167 bytes, with 127 stale files removed.

Result: `swift test` passed with 14 tests, one fixture-gated test skipped. The
copied-camera smoke command
`SDREVIEW_FIXTURE="/Users/vishaljain/Documents/Video editor/.local-test-data/sd-last-24h-20260705-1748" swift test --filter FixtureSmokeTests`
passed against the refreshed copied fixture. The smoke export now contains one
real JPG output, one real MOV passthrough segment, two output-hash manifest
entries, and zero manifest failures.

Interpretation: review-agent P1/P2 safety and correctness findings are mostly
addressed in code. Follow-up review found one remaining P1 around source-hash
validation for export resume; that was fixed with a regression test. Remaining
pending decisions are true cursor-anchored 100% photo zoom, full corrupt-media
scanner validation, and explicit resume offer versus auto-resume.
